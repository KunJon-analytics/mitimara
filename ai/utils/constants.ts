import { env } from "@/env.mjs";

export const baseURL = env.NEXT_PUBLIC_APP_URL;

export const namespace = "mitimura-testnet";

export const directoryPath = "content";

export const deployedSiteUrl = "https://mitimara.vercel.app/";
