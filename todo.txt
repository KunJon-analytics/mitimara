tree planting can only be done with user points greater than -10
    => tree loaction gotten from vercel
    => only when tree evidence is posted will code be shown
    => subtract points from user (prevents spamming)

3 verifications are to chosen (random or user chosses to) and 2 out of three has to authentic for tree to be marked authentic.
   

verifications can only be done when (also check in server while creating):
    => user must be within { 1km }sphere of tree location 
        =>then /tree/treeId/verify-tree page will be visible (use pi kyc way)
            => user visits page, 
            => compare location (allow if close, disallow if not)
            => show upload verification video url page (yes/no button)
    => user with points greater than -10
    => total verifications are less then 3
    => isauthentic/unauthentic verifications are less than 2

once isauthentic/nonauthentic is equal to 2 or total verification is 3:
    => mark now as date tree is verified.
    => send tree is verified event
        => event should set tree authenticity (true if greater than 1 iasuthentic verications)
        => update tree planter point 
            => if tree is authentic increment planter point && referrer planter point for referrer
            => else decrement planter point * 1.5
        => update verifier points 
            => if tree.isAuthentic === tree.isAuthentic (increment verifier point) && referrer verifier point for referrer
            => else decrement verifier point * 1.5

// add this system later (report tree verifications with point deduction system)
sceneraios where user upload bad image, when verification images are not completed
    => points will fight this, since users will be deducted points.

if a tree is not validated after a week, it is destroyed(uprooted add something catchy) and planter still looses points
so invite friends around you please (future plan)

signup and get 10 points.

There is a reward factor each for verifier and planter with which points deducted during planting 
or verifying is multiplied when tree is finally verified as authentic for the planter or verifier
submitted tree authenticity as the final authenticity fo the tree
Users can  edit tree only when there are no verifications
we will also transfer points to people who recognize and report foul play 
(so use our tg announcement channel to view evidence so they can be reported for points)
add city bounty contest (planter must have more points than normal) to roadmap 
add roadmap page


create report verification feature for planters of fake trees
create biggest earners leaderboard
remove testnet pi payment
add env
add prisma deploy to CI/CD pipeline

create points exchange list (user list, and only initiated list) and detail page 
    exchange point button should link to user list (on profile dropdown)

    user list page should show exchange points modal button 
        disabled when prechecks below are false
        alert message of reason when button disabled
        countdown to next claimmable
        alert points needed to enable exchange points
        alert pending exchange

        modal alert user that all available points will be forfeited if foul play is spotted
        for all planting / verification activities carried out between last claim / 
        account created date and today

    detail page should show all real trees planted or verified by user within stipulated time
        show link to tree 
        
        




for points exchange creation
    pre-checks 
        user must have above subscription points
        there must be no pending points exchange
        previous exchange / account must be at least a month old 
        must have planted or verified a real tree within stipulated time

    creation
        create exchange coefficient func = planter or verifier pot balance / total user points
        calculate claimmable pi amount (user points - 10) * exchange coefficient
        get last claimed date (latest uesr exchange or created date )
        set user points to 10


    post-creation
        send link of exchange to notif group
        send a create payment event that will start in 24 hours if exchange is VERIFIED
        mark exchange as PAYMENT_CREATED

pre deploy
    remove testnet from environment var
    add pub telegram group to env

// create donate page and link users to the page throughout the app (fix links apart)

https://www.prisma.io/docs/orm/prisma-client/queries/filtering-and-sorting

user trees should show unverified trees and unarchived fake trees (done)

archive fake/unverified trees older than 3 months (done)

create admin page to reward/penalize user with points (policing/ecosystem) (done)

add tree evidences and report tree link for real tree notification (done).

add report tree modal to fake mature tree detail page when planter logged in (done)

add resolve tree port modal button for tree with pending report for admin (done)

test new functions

create new blog articles and hyperlink them and train ai

open pull request for pi app licence github

message anthony for pull and community policing chat (ready to move to fireside/chat if api)

Tree Report:
    - There can be max 1 report for a MATURE tree (creation)
    - report requires text explaining reason for report (include link if available)
    - needs id, related tree, reporter, status (PENDING, AGREED, DECLINED), resolver
    - status only changed during resolve by admin
    
    
    Real Tree: 
        Only created by non planter and non verifier with 1+ policing points
        subtracts policing point (1)
        sends report link to team group (can be changed to private policing group)


    Fake Tree:
    - created by only planter with 1+ planter/verifier point
    - subtracts point (1)
    - sends report link to team group (can be changed to private policing group)
    
    Resolution:
    - only admin can for PENDING report
    - DECLINED report of REAL tree requires no further action
    - DECLINED report of FAKE tree 
        - archive tree
    - AGREED report of FAKE tree (reported by planter)
        - add planter points for planter
        - add verifier points for verifier with treeIsAuthentic = true 
        - subtract (verifier points + penalty) for verifiers with treeIsAuthentic = false 
    - AGREED report of REAL tree (reported by non participating user)
        - add policing point (1 * 2) for reporter 
        - subtract planter points
        - add verifier points for verifier with treeIsAuthentic = false 
        - subtract (verifier points + penalty) for verifiers with treeIsAuthentic = true 